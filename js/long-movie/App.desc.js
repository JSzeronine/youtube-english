

( function () {
    const index = function () {

        function hide(){
            
        }

        function Init(callback) {
        }

        return{
            Init,
            hide
        }
    }

    $(document).ready(() => {
        App.desc = index(); 
    });

})();